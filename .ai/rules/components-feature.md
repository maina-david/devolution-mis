---
paths:
  - 'app/Http/Controllers/EvidenceController.php,resources/js/components/evidence-row-action.tsx,tests/Feature/DocumentRecordsGovernanceTest.php'
---

# Components Feature

## Preview governed multimedia through range-capable native controls
Clean, checksum-valid audio/video documents and retained versions must use the same county-scoped audited preview boundary with HTTP byte-range support. Render video/audio through native controls in the shared evidence Sheet; keep static documents in the iframe and never expose quarantined, tampered, unsupported or out-of-scope media.
